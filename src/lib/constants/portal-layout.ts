/** Shared page frame for Merchant / Agent portal content. */
export const PORTAL_PAGE_CLASS =
  "mx-auto flex w-full min-w-0 max-w-[1400px] flex-col gap-4 px-3 py-4 sm:gap-5 sm:px-5 sm:py-6";

/** Filter / search panel grid. */
export const PORTAL_FILTER_CLASS =
  "grid min-w-0 gap-3 rounded-lg border border-edge bg-elevated p-3 sm:grid-cols-2 sm:p-4 lg:grid-cols-4";

/** Actions row inside filter panel. */
export const PORTAL_FILTER_ACTIONS_CLASS =
  "flex flex-wrap items-end gap-2 sm:col-span-2 lg:col-span-1";
